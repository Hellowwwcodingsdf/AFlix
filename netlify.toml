[build]
  publish = "."
  command = "chmod +x generate-env.sh && ./generate-env.sh"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Environment variables to configure in Netlify Dashboard:
# TMDB_API_KEY - Your TMDB API key (required)
